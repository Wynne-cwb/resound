# macOS
.DS_Store

# Xcode / Swift
.build/
build/
DerivedData/
*.xcuserstate
*.xcworkspace/xcuserdata/
*.xcodeproj/xcuserdata/
.swiftpm/
Package.resolved

# Derived index (NEVER commit — rebuildable from vault)
*.sqlite
*.sqlite-wal
*.sqlite-shm
index/

# Local config / secrets (API keys, vault paths)
.env
*.local
secrets.*

# Vault working copies (lives in App Support, not in code repo)
vaults/

# Python diarization 实验(venv+模型，派生物)
experiments/diar-py/.venv/
experiments/diar-py/models/

# sherpa-onnx 静态库(派生物，~百MB，用 scripts/build-sherpa-onnx.sh 重建)
Vendor/sherpa-onnx/

# Python bytecode
__pycache__/
*.pyc
